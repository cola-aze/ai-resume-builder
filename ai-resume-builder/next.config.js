/** @type {import('next').NextConfig} */
const nextConfig = {
    experimental: {
        // appDir: true,
        // 如果使用Turbopack，可以尝试禁用
        // turbopack: false
    },
    // 其他配置...
};

module.exports = nextConfig;
